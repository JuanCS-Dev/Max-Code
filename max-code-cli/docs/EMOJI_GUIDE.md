# Max-Code CLI - Emoji Guide

**"Cada agente tem sua identidade visual"**

Este documento define os emojis oficiais usados em todo o Max-Code CLI para criar uma experiência visual rica no terminal.

---

## Agents (Port 8160-8167)

### 🌳 PlanAgent (Port 8160)
**Emoji:** 🌳 (Árvore)
**Uso:** Tree of Thoughts, planejamento, exploração de alternativas

```python
print(f"   🌳 Phase 1: Tree of Thoughts exploration...")
print(f"   🌳 Generating 3 alternative approaches...")
```

### 🔍 ExploreAgent (Port 8161)
**Emoji:** 🔍 (Lupa)
**Uso:** Exploração de codebase, busca, descoberta

```python
print(f"   🔍 Exploring codebase...")
print(f"   🔍 Searching for patterns...")
```

### 💻 CodeAgent (Port 8162)
**Emoji:** 💻 (Computador)
**Uso:** Geração de código

```python
print(f"   💻 Phase 1: Generating code...")
print(f"   💻 Creating implementation...")
```

### 🔴 TestAgent (Port 8163)
**Emoji:** 🔴 (RED), ✅ (GREEN), 🔄 (REFACTOR)
**Uso:** Ciclo TDD completo

```python
print(f"   🔴 Phase 1: RED - Writing tests...")
print(f"   🧠 Phase 2: MAXIMUS edge case prediction...")
print(f"   ✅ Phase 3: GREEN - Running tests...")
print(f"   🔄 Phase 4: REFACTOR - Optimizing...")
```

### 🔍 ReviewAgent (Port 8164)
**Emoji:** 🏛️ (Templo - Constitutional), ⚖️ (Balança - Ethical)
**Uso:** Code review constitucional e ético

```python
print(f"   🏛️ Phase 1: Constitutional review (P1-P6)...")
print(f"   ⚖️ Phase 2: MAXIMUS ethical review (4 frameworks)...")
print(f"   🔀 Phase 3: Fusion...")
```

### 🔧 FixAgent (Port 8165)
**Emoji:** 🔧 (Chave inglesa), 🏥 (Hospital - healing)
**Uso:** Bug fixing e root cause analysis

```python
print(f"   🔧 Phase 1: Quick fix attempt...")
print(f"   🏥 Phase 2: PENELOPE root cause analysis...")
```

### 📝 DocsAgent (Port 8166)
**Emoji:** 📝 (Memo), 📖 (Livro - narrative)
**Uso:** Documentação e narrativa

```python
print(f"   📝 Phase 1: Generating standard docs...")
print(f"   📖 Phase 2: NIS narrative generation...")
```

### 👑 ArchitectAgent - Sophia (Port 8167)
**Emoji:** 👑 (Coroa - Wisdom Queen)
**Uso:** Decisões arquiteturais estratégicas

```python
print(f"   👑 SOPHIA - A Arquiteta (Strategic Analysis)")
print(f"   📊 Phase 1: MONITOR - Understanding the problem...")
print(f"   🌳 Phase 2: EXPLORE - Tree of Thoughts...")
print(f"   🔴 Phase 4: RED TEAM - Adversarial criticism...")
print(f"   ⚖️  Phase 5: FUSION - Selecting best approach...")
print(f"   📝 Phase 6: DOCUMENT - Creating ADR...")
```

---

## MAXIMUS Integration Services

### 🧠 MAXIMUS Core (Port 8153)
**Emoji:** 🧠 (Cérebro)
**Uso:** Análise sistêmica central

```python
print(f"   🧠 MAXIMUS systemic analysis...")
```

### 🏥 PENELOPE (Port 8150)
**Emoji:** 🏥 (Hospital)
**Uso:** Code healing e root cause analysis

```python
print(f"   🏥 PENELOPE root cause analysis...")
```

### 🎯 MABA (Port 8151)
**Emoji:** 🎯 (Alvo)
**Uso:** Bias detection e mitigation

```python
print(f"   🎯 MABA bias analysis...")
```

### 📖 NIS (Port 8152)
**Emoji:** 📖 (Livro aberto)
**Uso:** Narrative Intelligence System

```python
print(f"   📖 NIS narrative generation...")
```

---

## Status & Progress Indicators

### ✅ Success / Completion
```python
print(f"✅ Task completed successfully")
print(f"✅ All tests passed")
print(f"✅ Constitutional compliance verified")
```

### ⚠️ Warning / Fallback
```python
print(f"⚠️ MAXIMUS offline, using fallback")
print(f"⚠️ Security issues found: {count}")
print(f"⚠️ Constitutional violations detected")
```

### ❌ Error / Rejection
```python
print(f"❌ Task failed: {error}")
print(f"❌ Code rejected by Constitutional review")
```

### 🔀 Fusion / Decision
```python
print(f"   🔀 Phase 3: Fusion...")
print(f"   🔀 Combining Max-Code + MAXIMUS...")
```

### 📊 Metrics / Analysis
```python
print(f"📊 Metrics Tracker:")
print(f"   LEI: {lei:.2f}")
print(f"   FPC: {fpc:.1%}")
```

### 🏆 Winner / Best Option
```python
print(f"   🏆 Selected: Option 1 (confidence: 0.92)")
```

### 🚀 Launch / Start
```python
print(f"🚀 Max-Code CLI v3.0 starting...")
```

### 💾 Cache / Storage
```python
print(f"💾 Cache hit: analysis-abc123")
```

### ⏱️ Performance / Time
```python
print(f"⏱️ Execution time: 2.3s")
```

---

## Constitutional Principles (P1-P6)

### 🎯 P1 - Completude Obrigatória
**Emoji:** 🎯 (Alvo - completude)

```python
print(f"🎯 P1: Checking for placeholders...")
```

### ✓ P2 - Validação Preventiva
**Emoji:** ✓ (Check)

```python
print(f"✓ P2: Validating API existence...")
```

### 🤔 P3 - Ceticismo Crítico
**Emoji:** 🤔 (Pensando)

```python
print(f"🤔 P3: Questioning assumptions...")
```

### 📍 P4 - Rastreabilidade Total
**Emoji:** 📍 (Pin)

```python
print(f"📍 P4: Creating ADR for traceability...")
```

### 🌐 P5 - Consciência Sistêmica
**Emoji:** 🌐 (Globo - systemic)

```python
print(f"🌐 P5: Analyzing systemic impact...")
```

### ⚡ P6 - Eficiência de Token
**Emoji:** ⚡ (Raio - eficiência)

```python
print(f"⚡ P6: Optimizing token usage...")
```

---

## DETER-AGENT Layers

### 🏛️ Constitutional Layer
**Emoji:** 🏛️ (Templo)

### 💭 Deliberation Layer (ToT, CoT)
**Emoji:** 💭 (Nuvem de pensamento)

### 💾 State Management Layer
**Emoji:** 💾 (Disquete - state)

### ⚙️ Execution Layer
**Emoji:** ⚙️ (Engrenagem)

### 🎁 Incentive Layer (Rewards)
**Emoji:** 🎁 (Presente)

---

## Usage Pattern

### Agent Execution Template
```python
async def _execute_async(self, task: AgentTask) -> AgentResult:
    print(f"\n{'='*70}")
    print(f"   👑 AGENT_NAME - Description")
    print(f"   'Biblical quote'")
    print(f"{'='*70}\n")

    # Phase 1: Max-Code
    print(f"   🌳 Phase 1: Max-Code processing...")

    # Phase 2: MAXIMUS (optional)
    if self.maximus_client:
        if await self.maximus_client.health_check():
            print(f"   🧠 Phase 2: MAXIMUS analysis...")
        else:
            print(f"   ⚠️ MAXIMUS offline, using fallback")

    # Phase 3: Fusion
    print(f"   🔀 Phase 3: Fusion...")

    print(f"\n{'='*70}\n")

    return AgentResult(...)
```

---

## Tree of Thoughts Visualization

```python
print(f"🌳 Tree of Thoughts: Generating {num} alternative approaches...")
print(f"   Problem: {problem[:80]}...")
print(f"   ✓ Generated {len(thoughts)} thoughts")
print(f"   ✓ Evaluated all thoughts")
print(f"\n   📊 Top {num} Thoughts:")
for i, thought in enumerate(top_thoughts, 1):
    print(f"   {i}. {thought.description[:60]}... (score: {thought.confidence:.2f})")
print(f"\n   🏆 Selected: {best.description[:60]}...")
```

---

## Red Team Criticism Pattern

```python
print(f"   🔴 Phase 4: RED TEAM - Adversarial criticism...")
for i, option in enumerate(options, 1):
    criticisms = self._red_team_criticism(option)
    print(f"      ├─ Option {i}: {len(criticisms)} concerns identified")

for criticism in criticisms:
    print(f"   ⚠️ [{criticism['risk_level']}] {criticism['question']}")
```

---

## Best Practices

1. **Consistência**: Sempre use o mesmo emoji para a mesma função
2. **Hierarquia Visual**: Use indentação com `   ` (3 espaços) para hierarquia
3. **Separadores**: Use `{'='*70}` para separar seções importantes
4. **Context**: Emoji deve tornar óbvio o que está acontecendo
5. **Performance**: Emojis no terminal não impactam performance

---

## Color Guidelines (Future)

Quando adicionar cores ao terminal:
- 🟢 Green: Success, approved, safe
- 🟡 Yellow: Warning, attention needed
- 🔴 Red: Error, critical, rejected
- 🔵 Blue: Information, neutral
- 🟣 Purple: MAXIMUS-specific actions

---

**Atualizado:** 2025-11-04
**Versão:** 1.0.0
